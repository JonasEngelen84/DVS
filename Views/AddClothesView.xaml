<UserControl x:Class="DVS.Views.AddClothesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:components="clr-namespace:DVS.Components"
             mc:Ignorable="d" 
             d:DesignHeight="350"
             d:DesignWidth="203"
             Background="#FFE2F2FF"
             x:Name="_AddClothesView">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text=" Kleidung Hinzufügen "
                   HorizontalAlignment="Center"
                   FontSize="20"
                   Foreground="#FF1448EA" />

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>

                <components:ClothesCategorieCheckboxComponent Grid.Column="0" />
                <components:ClothesSeasonChoiceComponent Grid.Column="1" />
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Name:"
                           Margin="8 5 0 0"
                           Style="{StaticResource ModalTextStyle}" />

                <TextBox Grid.Column="1"
                         Height="15"
                         Width="149"
                         VerticalAlignment="Bottom"/>
            </Grid>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Größe:"
                           Margin="8 5 0 0"
                           Style="{StaticResource ModalTextStyle}" />

                <TextBox Grid.Column="1"
                         Height="15"
                         Width="149"
                         VerticalAlignment="Bottom" />
            </Grid>

            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Menge:"
                           Margin="8 5 0 0"
                           Style="{StaticResource ModalTextStyle}" />

                <TextBox Grid.Column="1"
                         Height="15"
                         Width="149"
                         VerticalAlignment="Bottom" />
            </Grid>

            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Kommentar:"
                           Margin="8 5 0 0"
                           Style="{StaticResource ModalTextStyle}" />

                <TextBox Grid.Column="1"
                         Height="15"
                         Width="115"
                         VerticalAlignment="Bottom" />
            </Grid>
        </Grid>
    </Grid>
</UserControl>
