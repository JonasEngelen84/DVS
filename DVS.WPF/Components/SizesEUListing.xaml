<UserControl x:Class="DVS.WPF.Components.SizesEUListing"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DVS.WPF.Components"
             xmlns:componentmodel="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             mc:Ignorable="d"
             DataContext="{Binding SizesCategoriesSeasonsListingViewModel}">

    <UserControl.Resources>
        <CollectionViewSource x:Key="SortedLoadedSizesEU"
                              Source="{Binding LoadedSizesEU}">
            <CollectionViewSource.SortDescriptions>
                <componentmodel:SortDescription PropertyName="Size"
                                                Direction="Ascending" />
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
    </UserControl.Resources>

    <Border BorderBrush="#1c1c1c"
            BorderThickness="0.75"
            CornerRadius="5"
            SnapsToDevicePixels="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40" />
                    <ColumnDefinition Width="25" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
            
                <TextBlock Grid.Column="0" Text="Größe" FontWeight="Bold" Margin="5 0 0 0" />
                <TextBlock Grid.Column="1" Text="St." FontWeight="Bold" Margin="10 0 0 0" />
                <TextBlock Grid.Column="2" Text="Kommentar" FontWeight="Bold" Margin="16 0 0 0" />
            </Grid>

            <!-- List -->
            <ListView ItemsSource="{Binding LoadedSizesEU}"
                      Grid.Row="1"
                      BorderThickness="0"
                      Background="Transparent"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <local:SizeListingItem />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Border>
</UserControl>
